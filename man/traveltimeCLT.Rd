% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traveltimeCLT.R
\name{traveltimeCLT}
\alias{traveltimeCLT}
\title{Travel time estimation modelling using the Central Limit Theorem}
\usage{
traveltimeCLT(
  data,
  model = c("trip-specific", "population"),
  estimate = c("both", "mean-only"),
  lag = 1L,
  nsamples = 500L,
  min.links = 5L,
  timebin_rules = NULL
)
}
\arguments{
\item{data}{A data frame of trips and their road level travel information, formated as \code{trips}, see \code{trips} or \code{View(data(trips))}.}

\item{model}{Specifies whether the \code{trip-specific} or \code{population} models should be used.}

\item{estimate}{Specifies whether to estimate \code{both} the mean and variance or \code{mean-only}. Only applied with \code{model=trip-specific}.}

\item{lag}{Maximum lag at which to calculate the autocorrelations. Default is 1 for the first order-autocorrelations.}

\item{nsamples}{The number of trips to sample for parameter estimation.}

\item{min.links}{The minimum number of links in each of the sampled trip.}

\item{timebin_rules}{A list containing, start, end, days and tag for each timebin of the dataset (see example in \code{time_bins}).}
}
\value{
Returns a list of the network parameters (namely the mean and standard deviation of travel time, estimated based on the train data set (see \code{link_mean_variance})) required for estimating travel times for the test data set.
}
\description{
This function allows for the calculation of travel time estimation confidence intervals on the test data set.
}
\examples{
\dontrun{

fit <- traveltimeCLT(train, lag = 1)
}

}
