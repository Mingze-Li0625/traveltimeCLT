% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_preparation.R
\name{get_timeBin_x_edges}
\alias{get_timeBin_x_edges}
\title{Generate time-bin \eqn{\times} edge statistics table from trip data}
\usage{
get_timeBin_x_edges(
  trips = NULL,
  tripID = NULL,
  linkId = NULL,
  length = NULL,
  timeBin = NULL,
  time = NULL,
  duration = NULL,
  log_duration = NULL
)
}
\arguments{
\item{trips}{A \code{data.table} containing trips data. Column names are case-insensitive.
Expected it contains: trip, linkID, length; time, or duration/logduration+timeBin. See details.}

\item{tripID}{Vector of trip ID (if \code{trips} is not provided).}

\item{linkId}{Vector of edge ID.}

\item{length}{Vector of edge lengths.}

\item{timeBin}{Vector of pre-computed time bins. We recommand use the function \code{\link{time_bins_readable}}.}

\item{time}{Vector of time the car enter the edge.}

\item{duration}{Vector of measured durations of finish the edge.}

\item{log_duration}{Vector of log durations.}
}
\value{
A \code{data.table} with columns:
\itemize{
\item \code{linkId}: Identifier for the edge.
\item \code{timeBin}: Time period the trip is in. The "Global" timeBin means
Use all observations passed the edge.
\item \code{mean}: Mean log-duration.
\item \code{sd}: Standard deviation of log-duration (returns 0 for single observations).
\item \code{frequency}: Number of observations.
\item \code{length}: The edge length, calculated by mode.
\item \code{ID}: Unique identifier for the edge and timeBin.
}
}
\description{
This function processes the trip data to statistics (mean and sd of log duration, frequency, length)
categorized by edges and time bins. It accept both vector input and data table (\code{trips})
}
\details{
\itemize{
\item If \code{trips} is provided: Column names are case-insensitive.
\item If \code{trips} is not provided: All vector parameters must be equal-length
\item This function will use \code{time} if provided. Otherwise it will use \code{log_duration}, and then \code{duration}.
}

The input need to fulfill:
\itemize{
\item Time parameters: \code{timeBin} + (\code{duration} or \code{log_duration}), OR \code{time}.
This function will use time if provided.
\item Trip parameters: \code{trip}, \code{linkId}, \code{length}. Column \code{trip} in trips refer the trip ID.
}
}
\examples{
#Use a data table
data(trips)
names(trips)<-c("trip","linkid","timebin","speed","duration","length","time")
stat<-get_timeBin_x_edges(trips)
View(stat)
# Using raw parameters
stat2=get_timeBin_x_edges(tripID=trips$trip,time=trips$time,linkId=trips$linkid,length = trips$length)
View(stat2)
}
\seealso{
\code{\link{get_timeBin_x_connections}} Generate time-bin \eqn{\times} connection statistics table from trip data
}
\author{
Mingze Li \href{mailto:mingzeli7@cmail.carleton.ca}{mingzeli7@cmail.carleton.ca}
}
