% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_trips.R
\name{sample_trips}
\alias{sample_trips}
\title{Sampling a random set of trips from a data set}
\usage{
sample_trips(data, nsamples = 100L, min.links = NULL)
}
\arguments{
\item{data}{A data frame of trips and their road level travel information, formated as \code{trips}, see \code{trips} or \code{View(data(trips))}.}

\item{nsamples}{as scalar representing the number of trips to sample. Default is 100.}

\item{min.links}{as minimum number of links in each sampled trip.}
}
\value{
\code{sample_trips} return a vector of \code{tripID}'s of the samples trips.
}
\description{
\code{sample_trips} samples a random set of trips from \code{data}.
}
\examples{
\dontrun{
data(trips)
# sampling a 100 random trips with minium of 10 links each.
index = sample_trips(trips, min.links = 10)
trips[trips$tripID \%in\% index, ]
}
}
